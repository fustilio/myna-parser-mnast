{
  "name": "myna-parser-mnast-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo for Myna parser and Unist compatibility packages",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "bench": "pnpm -r run bench"
  },
  "devDependencies": {
    "pnpm": "^8.0.0"
  },
  "license": "MIT"
}
